import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  form: {
    margin: 30
  },
  field: {
      marginTop: 20,
      fontWeight: 'bold',
      fontSize: 20
  },
  textField: {
      fontSize: 16,
      marginBottom: 20
  }
})

export default styles